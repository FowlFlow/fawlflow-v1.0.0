# Copy this file to .env and fill in real values. Never commit .env itself.

# Postgres connection string (local Docker Postgres for dev, or your Neon connection string in production)
DATABASE_URL="postgresql://postgres:devpassword@localhost:5432/fowlflow?schema=public"

# Generate with: npx auth secret
AUTH_SECRET=""
